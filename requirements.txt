# CPU-only torch: sentence-transformers below has no torch pin, so pip would
# otherwise resolve the default CUDA-enabled wheel (2GB+). On Streamlit
# Community Cloud (no Dockerfile — this file drives the build directly, and
# the free tier is capped at 1GB RAM) that's the same slow-build/bloat trap
# that broke the HF Spaces deploy. Same fix here: pin CPU-only first.
--extra-index-url https://download.pytorch.org/whl/cpu

# LLM backends (at least one API key required for live cycles)
# Option A (FREE): set GROQ_API_KEY — https://console.groq.com
groq>=0.9
# Option B (paid): set ANTHROPIC_API_KEY — https://console.anthropic.com
anthropic>=0.40

# Data models & config
pydantic>=2.6
sqlmodel>=0.0.22
PyYAML>=6.0
python-dotenv>=1.0

# Signal ingestion
feedparser>=6.0
requests>=2.31
tavily-python>=0.5   # optional: job profile enrichment via web search (TAVILY_API_KEY)

# Evolution agent (unsupervised layer)
scikit-learn>=1.4
scipy>=1.13
numpy>=1.26

# Semantic search (job radar — multilingual sentence embeddings)
sentence-transformers>=3.0

# Publishing
markdown>=3.5

# Dashboard / Hugging Face Spaces (streamlit run app.py or dashboard.py)
-r requirements-dashboard.txt
